# 🚀 دليل النشر على Railway

## الخطوات السريعة:

### 1. رفع المشروع على GitHub
```bash
git init
git add .
git commit -m "Ready for Railway deployment"
git branch -M main
git remote add origin YOUR_GITHUB_REPO_URL
git push -u origin main
```

### 2. النشر على Railway

1. اذهب إلى [railway.app](https://railway.app)
2. سجل دخول بحساب GitHub
3. اضغط **"New Project"**
4. اختر **"Deploy from GitHub repo"**
5. اختر المستودع الخاص بك
6. Railway سيقوم تلقائياً بـ:
   - اكتشاف أنه مشروع Node.js
   - تثبيت الحزم (`npm install`)
   - بناء المشروع (`npm run build`)
   - تشغيل السيرفر (`npm start`)

### 3. الحصول على الرابط

- بعد النشر، Railway سيعطيك رابط مباشر للمشروع
- يمكنك أيضاً إضافة دومين مخصص من إعدادات المشروع

## ✅ التحقق من النشر:

- تأكد أن البناء نجح (Build succeeded)
- تأكد أن السيرفر يعمل (Deploy succeeded)
- افتح الرابط المقدم من Railway

## 🔧 ملاحظات مهمة:

- **Port**: Railway يحدد المنفذ تلقائياً عبر متغير البيئة `PORT`
- **Build**: يتم البناء تلقائياً عند كل push جديد
- **Logs**: يمكنك رؤية السجلات في Railway Dashboard

## 🐛 حل المشاكل:

إذا واجهت مشاكل:
1. تحقق من السجلات (Logs) في Railway
2. تأكد أن `dist` folder موجود بعد البناء
3. تأكد أن `PORT` متغير البيئة مضبوط (Railway يضبطه تلقائياً)

---

**جاهز للنشر! 🎉**

